---
title: 只是开始
author: 徐伟云
date: '2016-02-05'
slug: 只是开始
categories:
  - HTML
tags:
  - 标签功能
---

```{r}
library(ggplot2)
p <- ggplot(mpg, aes(class, hwy))
p + geom_boxplot() + ggtitle("火车fadrivr啦")
```

```{r}
plot(rnorm(100), main = "another图片")
```

```{r}
library(recharts)
echart(iris, ~ Sepal.Length, ~ Sepal.Width)
```

